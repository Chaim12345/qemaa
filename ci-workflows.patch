diff --git a/.github/workflows/android-build.yml b/.github/workflows/android-build.yml
index aa12c17..8d5383e 100644
--- a/.github/workflows/android-build.yml
+++ b/.github/workflows/android-build.yml
@@ -19,6 +19,9 @@ env:
   ANDROID_NDK_VERSION: "30-beta3"
   ANDROID_API: "36"
   ALPINE_MIRROR: "https://dl-cdn.alpinelinux.org/alpine/latest-stable"
+  DISTRO_ARTIFACT_NAME: linux-vm-distro-image
+  APK_ARTIFACT_NAME: linux-vm-release-apk
+  DEBUG_APK_ARTIFACT_NAME: linux-vm-debug-apk
 
 jobs:
   build:
@@ -109,17 +112,10 @@ jobs:
           test -s "${alpine_dir}/initramfs-lts"
           du -h "${alpine_dir}"/*
 
-      - name: Prepare Secrets and Keystore
+      - name: Prepare Keystore
         shell: bash
         run: |
           set -euo pipefail
-          # Create .env from .env.example if missing (for Secrets Gradle Plugin).
-          if [ ! -f .env ] && [ -f .env.example ]; then
-            cp .env.example .env
-          elif [ ! -f .env ]; then
-            touch .env
-          fi
-
           if [ -f debug.keystore.base64 ]; then
             base64 --decode debug.keystore.base64 > debug.keystore 2>/dev/null || \
               base64 -d debug.keystore.base64 > debug.keystore
@@ -147,8 +143,8 @@ jobs:
           fi
           chmod +x gradlew
 
-      - name: Build Release APK with bundled QEMU
-        run: ./gradlew testDebugUnitTest assembleRelease --no-daemon --parallel --build-cache --stacktrace
+      - name: Build APKs and run unit tests
+        run: ./gradlew testDebugUnitTest assembleRelease assembleDebug --no-daemon --parallel --build-cache --stacktrace
 
       - name: Verify bundled VM assets
         shell: bash
@@ -162,11 +158,70 @@ jobs:
           unzip -l "${apk}" | grep -E 'lib/arm64-v8a/libqemu-system-x86_64\.so$'
           unzip -l "${apk}" | grep -E 'assets/alpine/(vmlinuz-lts|initramfs-lts)$'
           unzip -l "${apk}" | grep -E 'assets/qemu/pc-bios/(bios-256k\.bin|linuxboot_dma\.bin|efi-virtio\.rom)$'
+          unzip -l "${apk}" | grep -E 'assets/xterm/xterm\.js$'
 
       - name: Upload Release APK Artifact
         uses: actions/upload-artifact@v4
         with:
-          name: linux-vm-release-apk
+          name: ${{ env.APK_ARTIFACT_NAME }}
           path: app/build/outputs/apk/release/*.apk
           retention-days: 14
           if-no-files-found: error
+
+      - name: Upload Debug APK Artifact
+        uses: actions/upload-artifact@v4
+        with:
+          name: ${{ env.DEBUG_APK_ARTIFACT_NAME }}
+          path: app/build/outputs/apk/debug/*.apk
+          retention-days: 14
+          if-no-files-found: error
+
+  build-distro:
+    name: Build prebuilt distro image (Alpine + Node + Go + agents)
+    runs-on: ubuntu-latest
+    timeout-minutes: 60
+
+    steps:
+      - name: Checkout Repository
+        uses: actions/checkout@v4
+
+      - name: Free disk space for the image build
+        shell: bash
+        run: |
+          set -euo pipefail
+          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc \
+            /usr/local/.ghcup /usr/local/share/boost 2>/dev/null || true
+          df -h /
+
+      - name: Build rootfs, kernel and initramfs in Alpine
+        shell: bash
+        run: |
+          set -euo pipefail
+          mkdir -p out
+          # The script runs INSIDE the alpine container; the guest is x86_64,
+          # so the chroot is native (no qemu-user needed).
+          docker run --rm -v "$PWD:/work" -w /work alpine:3.22 \
+            sh .github/scripts/build-distro.sh
+
+      - name: Verify distro outputs
+        shell: bash
+        run: |
+          set -euo pipefail
+          test -s out/linux-vm-rootfs.img.gz
+          test -s out/linux-vm-rootfs.img.gz.sha256
+          test -s out/linux-vm-vmlinuz-lts
+          test -s out/linux-vm-initramfs-lts
+          test -s out/linux-vm-distro-manifest.txt
+          echo "== manifest =="
+          cat out/linux-vm-distro-manifest.txt
+          echo "== sizes =="
+          du -sh out/*
+          df -h /
+
+      - name: Upload Distro Image Artifact
+        uses: actions/upload-artifact@v4
+        with:
+          name: ${{ env.DISTRO_ARTIFACT_NAME }}
+          path: out/*
+          retention-days: 14
+          if-no-files-found: error
diff --git a/.github/workflows/android-cd.yml b/.github/workflows/android-cd.yml
deleted file mode 100644
index 0da21bf..0000000
--- a/.github/workflows/android-cd.yml
+++ /dev/null
@@ -1,164 +0,0 @@
-name: "CD: Android QEMU APK"
-
-on:
-  push:
-    tags:
-      - "android-qemu-v*.*.*"
-
-concurrency:
-  group: ${{ github.workflow }}-${{ github.ref }}
-  cancel-in-progress: true
-
-permissions:
-  contents: write
-
-env:
-  QEMU_VERSION: "9.0.0"
-  ANDROID_NDK_VERSION: "30-beta3"
-  ANDROID_API: "36"
-  ALPINE_MIRROR: "https://dl-cdn.alpinelinux.org/alpine/latest-stable"
-
-jobs:
-  build-release:
-    name: Build and publish Android APK with bundled QEMU
-    runs-on: ubuntu-latest
-    timeout-minutes: 120
-
-    steps:
-      - name: Checkout Repository
-        uses: actions/checkout@v4
-
-      - name: Set up JDK 21
-        uses: actions/setup-java@v4
-        with:
-          distribution: "temurin"
-          java-version: "21"
-          cache: gradle
-
-      - name: Set up Gradle
-        uses: gradle/actions/setup-gradle@v4
-        with:
-          gradle-version: "9.3.1"
-
-      - name: Set up Docker Buildx
-        uses: docker/setup-buildx-action@v3
-
-      - name: Build Android QEMU image
-        uses: docker/build-push-action@v6
-        with:
-          context: .
-          file: .github/docker/Dockerfile.qemu
-          platforms: linux/amd64
-          build-args: |
-            QEMU_VERSION=${{ env.QEMU_VERSION }}
-            NDK_VERSION=${{ env.ANDROID_NDK_VERSION }}
-            ANDROID_API=${{ env.ANDROID_API }}
-          tags: qemu-android-builder:${{ github.sha }}
-          load: true
-          cache-from: type=gha,scope=qemu-android-multiabi
-          cache-to: type=gha,mode=max,scope=qemu-android-multiabi
-
-      - name: Copy QEMU binaries and firmware into the repo
-        shell: bash
-        run: |
-          set -euo pipefail
-          # The QEMU executables are packaged as native libraries so the installer
-          # extracts them into nativeLibraryDir. Apps targeting API 29+ cannot
-          # exec() files from their writable data directory.
-          # Built for BOTH host ABIs (x86_64 + arm64-v8a).
-          mkdir -p app/src/main/jniLibs/x86_64 app/src/main/jniLibs/arm64-v8a \
-            app/src/main/assets/qemu
-          container_id="$(docker create "qemu-android-builder:${GITHUB_SHA}")"
-          trap 'docker rm -f "${container_id}" >/dev/null 2>&1 || true' EXIT
-          for abi in x86_64 arm64-v8a; do
-            docker cp "${container_id}:/qemu/${abi}/qemu-system-x86_64" \
-              "app/src/main/jniLibs/${abi}/libqemu-system-x86_64.so"
-            chmod 0755 "app/src/main/jniLibs/${abi}/libqemu-system-x86_64.so"
-            file "app/src/main/jniLibs/${abi}/libqemu-system-x86_64.so"
-            test -s "app/src/main/jniLibs/${abi}/libqemu-system-x86_64.so"
-          done
-
-          # Firmware (SeaBIOS + linuxboot ROMs + virtio option ROM) ships as
-          # regular assets; QEMU receives the extracted dir via -L at runtime.
-          docker cp "${container_id}:/qemu/pc-bios" app/src/main/assets/qemu/
-          ls -la app/src/main/assets/qemu/pc-bios | head -25
-          test -s app/src/main/assets/qemu/pc-bios/bios-256k.bin
-          test -s app/src/main/assets/qemu/pc-bios/linuxboot_dma.bin
-          test -s app/src/main/assets/qemu/pc-bios/efi-virtio.rom
-
-      - name: Download Alpine Linux VM assets
-        shell: bash
-        run: |
-          set -euo pipefail
-          alpine_dir="app/src/main/assets/alpine"
-          mkdir -p "${alpine_dir}"
-
-          curl --fail --location --retry 5 --retry-all-errors --retry-delay 2 \
-            "${ALPINE_MIRROR}/releases/x86_64/netboot/vmlinuz-lts" \
-            --output "${alpine_dir}/vmlinuz-lts"
-          curl --fail --location --retry 5 --retry-all-errors --retry-delay 2 \
-            "${ALPINE_MIRROR}/releases/x86_64/netboot/initramfs-lts" \
-            --output "${alpine_dir}/initramfs-lts"
-
-          test -s "${alpine_dir}/vmlinuz-lts"
-          test -s "${alpine_dir}/initramfs-lts"
-          du -h "${alpine_dir}"/*
-
-      - name: Prepare Secrets and Keystore
-        shell: bash
-        run: |
-          set -euo pipefail
-          if [ ! -f .env ] && [ -f .env.example ]; then
-            cp .env.example .env
-          elif [ ! -f .env ]; then
-            touch .env
-          fi
-
-          if [ -f debug.keystore.base64 ]; then
-            base64 --decode debug.keystore.base64 > debug.keystore 2>/dev/null || \
-              base64 -d debug.keystore.base64 > debug.keystore
-          fi
-
-          if [ ! -f debug.keystore ] || [ ! -s debug.keystore ]; then
-            keytool -genkey -v \
-              -keystore debug.keystore \
-              -storepass android \
-              -alias androiddebugkey \
-              -keypass android \
-              -keyalg RSA \
-              -keysize 2048 \
-              -validity 10000 \
-              -dname "CN=Android Debug,O=Android,C=US"
-          fi
-          ls -lh debug.keystore
-
-      - name: Ensure Gradle Wrapper
-        shell: bash
-        run: |
-          set -euo pipefail
-          if [ ! -f gradlew ]; then
-            gradle wrapper --gradle-version 9.3.1 --distribution-type bin
-          fi
-          chmod +x gradlew
-
-      - name: Build Release APK with bundled QEMU
-        run: ./gradlew testDebugUnitTest assembleRelease --no-daemon --parallel --build-cache --stacktrace
-
-      - name: Verify bundled VM assets
-        shell: bash
-        run: |
-          set -euo pipefail
-          apk="$(find app/build/outputs/apk/release -maxdepth 1 -type f -name '*.apk' -print -quit)"
-          test -n "${apk}"
-          unzip -l "${apk}" | grep -E 'lib/x86_64/libqemu-system-x86_64\.so$'
-          unzip -l "${apk}" | grep -E 'lib/arm64-v8a/libqemu-system-x86_64\.so$'
-          unzip -l "${apk}" | grep -E 'assets/alpine/(vmlinuz-lts|initramfs-lts)$'
-          unzip -l "${apk}" | grep -E 'assets/qemu/pc-bios/(bios-256k\.bin|linuxboot_dma\.bin|efi-virtio\.rom)$'
-
-      - name: Create GitHub Release for tag
-        uses: softprops/action-gh-release@v2
-        with:
-          files: app/build/outputs/apk/release/*.apk
-          generate_release_notes: true
-        env:
-          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
diff --git a/.github/workflows/android-emulator-smoke.yml b/.github/workflows/android-emulator-smoke.yml
index 02be05b..2b198eb 100644
--- a/.github/workflows/android-emulator-smoke.yml
+++ b/.github/workflows/android-emulator-smoke.yml
@@ -32,6 +32,8 @@ env:
   APP_ACTIVITY: com.example.MainActivity
   BUILD_WORKFLOW_NAME: "Android CI & QEMU Build"
   APK_ARTIFACT_NAME: linux-vm-release-apk
+  DEBUG_APK_ARTIFACT_NAME: linux-vm-debug-apk
+  DISTRO_ARTIFACT_NAME: linux-vm-distro-image
 
 jobs:
   emulator-test:
@@ -73,6 +75,13 @@ jobs:
           gh run download "${run_id}" --name "${APK_ARTIFACT_NAME}" --dir apk
           ls -lh apk
 
+          # Distro image + debug APK for the prebuilt-distro test phases.
+          gh run download "${run_id}" --name "${DEBUG_APK_ARTIFACT_NAME}" --dir apk-debug \
+            || echo "::warning::no debug APK artifact (older build run?)"
+          gh run download "${run_id}" --name "${DISTRO_ARTIFACT_NAME}" --dir distro \
+            || echo "::warning::no distro artifact (older build run?)"
+          ls -lh apk-debug distro || true
+
       - name: Enable KVM for the emulator
         shell: bash
         run: |
@@ -109,6 +118,12 @@ jobs:
           force-avd-creation: false
           emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none -no-snapshot -memory 3072
           disable-animations: true
+          # The distro test decompresses a 3 GB disk image inside the emulator.
+          pre-emulator-launch-script: |
+            if [ -f ~/.android/avd/qemu-api36.avd/config.ini ]; then
+              grep -q "disk.dataPartition.size" ~/.android/avd/qemu-api36.avd/config.ini \
+                || echo "disk.dataPartition.size=12G" >> ~/.android/avd/qemu-api36.avd/config.ini
+            fi
           script: bash ${GITHUB_WORKSPACE}/.github/scripts/emulator-test.sh
 
       - name: Upload emulator evidence
diff --git a/.github/workflows/android-release.yml b/.github/workflows/android-release.yml
index 8c95430..20fdfa6 100644
--- a/.github/workflows/android-release.yml
+++ b/.github/workflows/android-release.yml
@@ -33,6 +33,7 @@ env:
   TEST_WORKFLOW_NAME: "Android Emulator Test"
   APK_ARTIFACT_NAME: linux-vm-release-apk
   EVIDENCE_ARTIFACT_NAME: emulator-test-evidence
+  DISTRO_ARTIFACT_NAME: linux-vm-distro-image
 
 jobs:
   tag-and-release:
@@ -163,12 +164,22 @@ jobs:
           gh run download "${BUILD_RUN_ID}" --repo "${GITHUB_REPOSITORY}" --name "${APK_ARTIFACT_NAME}" --dir release
           ls -lh release
 
+          # The prebuilt distro image ships as release assets; the app's
+          # "Install Distro" button resolves exactly these asset names.
+          if gh run download "${BUILD_RUN_ID}" --repo "${GITHUB_REPOSITORY}" --name "${DISTRO_ARTIFACT_NAME}" --dir release; then
+            echo "Distro image attached to the release."
+          else
+            echo "::warning::No distro artifact in build run ${BUILD_RUN_ID} — releasing APK only."
+          fi
+
           # Attach the strongest emulator-test evidence to the release.
           mkdir -p evidence
           if gh run download "${EMU_RUN_ID}" --repo "${GITHUB_REPOSITORY}" --name "${EVIDENCE_ARTIFACT_NAME}" --dir evidence 2>/dev/null; then
             cp evidence/qemu-version.txt release/qemu-version.txt 2>/dev/null || true
             cp evidence/screen-at-login.png release/emulator-screen-at-login.png 2>/dev/null || true
             cp evidence/boot-progress.txt release/emulator-boot-progress.txt 2>/dev/null || true
+            cp evidence/screen-distro-booted.png release/emulator-screen-distro-booted.png 2>/dev/null || true
+            cp evidence/distro-toolchain.txt release/distro-toolchain.txt 2>/dev/null || true
           fi
           ls -lh release
 
@@ -194,17 +205,28 @@ jobs:
           Built from commit `@SHA@` (build run #@BUILD_RUN_NUMBER@) and validated by the
           [full emulator test](https://github.com/@REPO@/actions/runs/@EMU_RUN_ID@):
           the app installs on an API 36 emulator, starts QEMU from `nativeLibraryDir`,
-          boots the Alpine guest **all the way to the login prompt** and stops the VM cleanly.
+          boots the Alpine guest to the login prompt, and additionally **boots the
+          prebuilt distro image in-app and verifies node, npm, go, pi and opencode
+          inside the guest** before stopping everything cleanly.
 
           **What's inside the APK**
           - QEMU 9.0.0 system emulator for **both device architectures**: `x86_64` and `arm64-v8a`
             (packaged as native libraries, executed from `nativeLibraryDir` — required by Android 10+ W^X policy)
-          - Alpine Linux netboot kernel + initramfs, SeaBIOS/QEMU firmware
-          - The guest boots from the network (DHCP + `alpine_repo`) into a RAM-backed Alpine
-
-          Install `app-release.apk` on any 64-bit Android 16+ device. After tapping Start, the
-          first boot needs a few minutes (pure-software TCG emulation) and downloads the Alpine
-          base system over the network.
+          - A real xterm.js terminal (raw byte stream, full colors, TUI apps, Ctrl/Tab/arrow keys,
+            Termux-style extra keys row, copy/paste, pinch zoom)
+          - Alpine netboot kernel + initramfs fallback so the app works with zero downloads
+
+          **Prebuilt distro (one tap in the app: "Install Distro")**
+          - `linux-vm-rootfs.img.gz` — persistent Alpine disk with **Node.js, npm, Go, git, tmux,
+            vim/nano/htop** plus the **π coding agent** (`pi`) and **opencode** preinstalled
+          - `linux-vm-vmlinuz-lts` / `linux-vm-initramfs-lts` — kernel matched to the image
+          - `linux-vm-rootfs.img.gz.sha256` — checksum the app verifies while downloading
+          - `linux-vm-distro-manifest.txt` — exact preinstalled versions
+          - Files persist across reboots — it is a real disk, not a RAM disk
+
+          Install `app-release.apk` on any 64-bit Android 16+ device. Netboot Start works
+          immediately; for the full environment tap **Install Distro** once (~1 GB download,
+          ~3 GB disk), then Start. First boot takes a few minutes (pure-software TCG emulation).
           NOTES
 
           sed -i \
